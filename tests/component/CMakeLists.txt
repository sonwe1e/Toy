if(DVS_BUILD_ADAPTERS)
    add_subdirectory(platform)
    add_subdirectory(media)
endif()
if(DVS_BUILD_UI)
    add_subdirectory(ui)
endif()
if(DVS_BUILD_DESKTOP)
    add_subdirectory(shell_windows)
endif()
